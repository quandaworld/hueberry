<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
</head>
<body>
  <div>
    <h2>Create your account</h2>
    
    <% if (locals.error) { %>
      <div role="alert">
        <span><%= error %></span>
      </div>
    <% } %>

    <form action="/auth/register" method="POST">
      <div>
        <div>
          <label for="firstName">First Name</label>
          <input id="firstName" name="firstName" type="text" required placeholder="First Name" value="<%= locals.firstName || '' %>">
        </div>
        <div>
          <label for="lastName">Last Name</label>
          <input id="lastName" name="lastName" type="text" required placeholder="Last Name" value="<%= locals.lastName || '' %>">
        </div>
        <div>
          <label for="username">Username</label>
          <input id="username" name="username" type="text" required placeholder="Username" value="<%= locals.username || '' %>">
        </div>
        <div>
          <label for="password">Password</label>
          <input id="password" name="password" type="password" required placeholder="Password">
        </div>
      </div>

      <div>
        <button type="submit">Register</button>
      </div>
    </form>
    
    <div>
      <a href="/auth/login">Already have an account? Sign in</a>
    </div>
  </div>
</body>
</html>